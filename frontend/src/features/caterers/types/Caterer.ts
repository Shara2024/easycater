export type FeaturedCaterer = {
  id: string;
  name: string;
  badge: string;
  imageUrl: string;
  rating: number;
  reviewCount: number;
  priceRangeLKR: [number, number];
  cuisineTags: string[];
};
